function hasChildren = nodeHasChildren( node )
% Returns true if and only if this XML structure node (generated by the
% parseXML method) has child nodes.
numberOfChildren = length( node.Children );
hasChildren = ( numberOfChildren >= 1 );
