INCLUDE_DIRECTORIES(${OpenSim_SOURCE_DIR} ${OpenSim_SOURCE_DIR}/Vendors ${XERCES_INCLUDE_DIR})

IF(NOT OPENSIM_MINIMAL_BUILD)
SUBDIRS(testMultipleSdfastModels testXMLParsing)
ENDIF(NOT OPENSIM_MINIMAL_BUILD)
