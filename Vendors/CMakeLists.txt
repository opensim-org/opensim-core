#-----------------------------------------------------------------------------

IF(OPENSIM_MINIMAL_BUILD)
	IF(WIN32)
	SUBDIRS(CFSQP MotionAnalysis)
	ELSE(WIN32)
	SUBDIRS(CFSQP)
	ENDIF(WIN32)
ELSE(OPENSIM_MINIMAL_BUILD)
	IF(WIN32)
	SUBDIRS(CFSQP MotionAnalysis ${XERCES_DIR_NAME})
	ELSE(WIN32)
	SUBDIRS(CFSQP ${XERCES_DIR_NAME})
	ENDIF(WIN32)
ENDIF(OPENSIM_MINIMAL_BUILD)
